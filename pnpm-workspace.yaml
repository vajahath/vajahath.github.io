allowBuilds:
  esbuild: true
  sharp: true

# Security overrides for transitive deps (see `pnpm audit`).
# Remove an entry once the direct dependency ships the patched version itself.
overrides:
  yaml: ^2.9.0
  fast-xml-builder: ^1.3.1
  js-yaml@3: ^3.15.2
  fast-uri: ^3.1.7
  svgo: ^4.1.0
